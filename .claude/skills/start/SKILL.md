---
name: start
description: Starts the local development server for this project by running `npm start`. Use this skill whenever the user wants to run, start, preview, or test the site locally. Triggers on phrases like "start the server", "run it", "test it", "preview", "open in browser", or "how do I test it".
---

Run the dev server:

```bash
npm start
```

This starts `http-server` on port 9090. Tell the user to open `http://localhost:9090` in their browser.
